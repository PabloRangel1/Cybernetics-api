<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <link rel="icon" type="image/x-icon" href="/assets/cybericon.ico" th:href="@{/assets/cybericon.ico}">
    <title>Lista de Colaboradores</title>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Michroma&display=swap');

        html, body {
            height: 100%;
            margin: 0;
            padding: 0;
            font-family: 'Michroma', sans-serif;
            background-size: cover;
            color: #f0f0f0;
            position: relative;
            overflow: hidden;
        }

        body {
            overflow-y: auto;
        }

        body::before {
            content: "";
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            background-image: repeating-linear-gradient(
                to bottom,
                rgba(255, 255, 255, 0.015),
                rgba(0, 0, 0, 0.02) 2px,
                transparent 2px,
                transparent 4px
            );
            z-index: 1;
            backdrop-filter: brightness(1.2);
        }

        #bg-video {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            object-fit: cover;
            z-index: 0;
            filter: brightness(1.2) contrast(1.3) saturate(1.5) blur(0.2px);
        }

        .container {
            max-width: 960px;
            margin: 40px auto;
            background: rgba(0, 0, 0, 0.6);
            padding: 20px;
            border-radius: 14px;
            box-shadow: 0 0 20px rgba(0, 255, 255, 0.4);
            border: 1px solid transparent;
            background-clip: padding-box;
            position: relative;
            z-index: 2;
            animation: borderGlow 6s linear infinite;
        }

        @keyframes borderGlow {
            0% { box-shadow: 0 0 5px #0ff, 0 0 10px #0ff; }
            50% { box-shadow: 0 0 10px #f0f, 0 0 20px #f0f; }
            100% { box-shadow: 0 0 5px #0ff, 0 0 10px #0ff; }
        }

        h1 {
            text-align: center;
            font-size: 2.5rem;
            color: #00ffe5;
            text-shadow: 0 0 12px #00ffe5;
            margin-bottom: 30px;
            animation: flicker 3s infinite alternate;
        }

        @keyframes flicker {
            0% { opacity: 1; text-shadow: 0 0 10px #00ffe5; }
            50% { opacity: 0.85; text-shadow: 0 0 18px #ff00ff; }
            100% { opacity: 1; text-shadow: 0 0 10px #00ffe5; }
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background: rgba(255, 255, 255, 0.04);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
            border: 1px solid rgba(0, 255, 255, 0.2);
            animation: fadeInUp 1s ease;
        }

        @keyframes fadeInUp {
            from { transform: translateY(20px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        table th, table td {
            padding: 10px 12px;
            text-align: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            color: #e0f7fa;
            font-size: 0.9rem;
        }

        table th {
            background-color: rgba(0, 255, 255, 0.08);
            color: #00ffff;
            font-weight: bold;
            text-shadow: 0 0 6px #00ffff;
        }

        table tr:nth-child(even) {
            background-color: rgba(255, 255, 255, 0.02);
        }

        table tr:hover {
            background-color: rgba(255, 255, 255, 0.05);
            transition: background-color 0.3s ease;
        }

        .btn {
            padding: 6px 10px;
            border: none;
            border-radius: 6px;
            font-size: 0.75rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            text-decoration: none;
            display: inline-block;
            margin: 0 3px;
            box-shadow: 0 0 6px #000;
            animation: fadeIn 1s ease-in-out;
            opacity: 0.95;
            border: 2px solid transparent;
        }

        .btn:hover {
            opacity: 1;
            background: transparent !important;
            transform: scale(1.02);
        }

        .btn-detalhes {
            background: #00ffff;
            color: #000;
        }

        .btn-detalhes:hover {
            color: #00ffff;
            border: 2px solid #00ffff;
        }

        .btn-editar {
            background: #ffd700;
            color: #000;
        }

        .btn-editar:hover {
            color: #ffd700;
            border: 2px solid #ffd700;
        }

        .btn-deletar {
            background: #ff0040;
            color: #fff;
        }

        .btn-deletar:hover {
            color: #ff0040;
            border: 2px solid #ff0040;
        }

        .btn-add {
            background: #00ff99;
            color: #000;
            padding: 10px 20px;
            font-size: 0.9rem;
            border-radius: 10px;
            display: inline-block;
            margin-bottom: 30px;
            box-shadow: 0 0 10px #00ff99;
            transition: all 0.3s ease;
            animation: pulse 2s infinite;
        }

        .btn-add:hover {
            background: transparent;
            color: #00ff99;
            border: 2px solid #00ff99;
            box-shadow: 0 0 20px #00ff99;
        }

        @keyframes pulse {
            0% { box-shadow: 0 0 8px #00ff99; }
            50% { box-shadow: 0 0 16px #00ff99; }
            100% { box-shadow: 0 0 8px #00ff99; }
        }

        @keyframes fadeIn {
            0% { opacity: 0; transform: scale(0.9); }
            100% { opacity: 1; transform: scale(1); }
        }

        .linkedin-icon, .h2-icon, .swagger-icon {
            position: fixed;
            right: 10px;
            z-index: 9999;
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 6px 10px;
            border-radius: 8px;
            background-color: rgba(0, 0, 0, 0.3);
            transition: background-color 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
            text-decoration: none;
        }

        .linkedin-icon {
            top: 10px;
        }

        .h2-icon {
            top: 60px;
        }

        .swagger-icon {
            top: 110px;
        }

        .linkedin-icon:hover {
            transform: scale(1.1);
            background-color: rgba(0, 0, 0, 0.6);
            box-shadow: 0 0 12px #ff0000;
        }

        .h2-icon:hover {
            transform: scale(1.1);
            background-color: rgba(0, 0, 0, 0.6);
            box-shadow: 0 0 12px hotpink;
        }

        .swagger-icon:hover {
            transform: scale(1.1);
            background-color: rgba(0, 0, 0, 0.6);
            box-shadow: 0 0 12px #0ff, 0 0 18px #f0f;
        }

        .linkedin-icon img,
        .h2-icon img,
        .swagger-icon img {
            width: 32px;
            height: 32px;
        }

        .linkedin-icon span {
            color: red;
            font-weight: bold;
            font-size: 0.9rem;
        }

        .h2-icon span {
            color: hotpink;
            font-weight: bold;
            font-size: 0.9rem;
        }

        .swagger-icon span {
            color: #00ff99;
            font-weight: bold;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
<a href="https://www.linkedin.com/in/pablo-rangel-2261432bb/" target="_blank" class="linkedin-icon" title="Linkedin">
    <img src="/assets/silverhand.ico" alt="Linkedin" />
    <span>LinkedIn</span>
</a>

<a href="http://localhost:8080/h2-console" target="_blank" class="h2-icon" title="H2 Console">
    <img src="/assets/caveiraRosa.ico" alt="H2 Console" />
    <span>Banco de Dados</span>
</a>

<a href="http://localhost:8080/swagger-ui/index.html" target="_blank" class="swagger-icon" title="Swagger">
    <img src="/assets/swagger256.png" alt="Swagger Docs" />
    <span>Swagger Docs</span>
</a>

<video autoplay muted loop id="bg-video">
    <source src="/assets/night-city-cyberpunk-2077-moewalls-com.mp4" type="video/mp4">
    Seu navegador não suporta vídeos em HTML5.
</video>

<div class="container">
    <h1>List Cybernetics</h1>
    <a href="/emp/ui/adicionar" class="btn-add">+ Adicionar Cybernetic</a>
    <table>
        <thead>
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Email</th>
            <th>Ações</th>
        </tr>
        </thead>
        <tbody>
        <tr th:each="emp : ${employees}">
            <td th:text="${emp.id}"></td>
            <td th:text="${emp.nome}"></td>
            <td th:text="${emp.email}"></td>
            <td>
                <a th:href="@{/emp/ui/listar/{id}(id=${emp.id})}" class="btn btn-detalhes">Ver Detalhes</a>
                <a th:href="@{'/emp/ui/alterar/' + ${emp.id}}" class="btn btn-editar">Alterar</a>
                <a th:href="@{'/emp/ui/deletar/' + ${emp.id}}" class="btn btn-deletar">Deletar</a>
            </td>
        </tr>
        </tbody>
    </table>
</div>
</body>
</html>
